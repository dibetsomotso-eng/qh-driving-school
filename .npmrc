# Only fail/warn on moderate severity and above.
# 17 low-severity findings are in the @tootallnate/once -> firebase-admin -> genkit
# dependency chain. No fix is available without a breaking genkit downgrade.
# Will resolve automatically when google-gax bumps to v5 in firebase-admin.
audit-level=moderate
